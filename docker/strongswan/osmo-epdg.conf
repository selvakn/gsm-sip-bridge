# strongswan.d/charon/osmo-epdg.conf — the fork's own-ePDG plugin (for
# operators running a strongSwan-based ePDG themselves) is irrelevant here:
# we're a *client* connecting to a carrier's ePDG, not running one. Disabled
# per the osmocom wiki's Option 2 walkthrough (research.md item 1).
osmo-epdg {
    load = no
}
