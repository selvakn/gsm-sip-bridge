# SIP credentials
SIP_PASSWORD=changeme

# Discord webhook for SMS forwarding (leave empty to disable)
DISCORD_WEBHOOK_URL=

# Metrics server port override (default: 9091)
# METRICS_PORT=9091

# Grafana admin password (docker-compose only)
GRAFANA_ADMIN_PASSWORD=admin

# VoWiFi/ePDG tunnel engine (only used when [vowifi].enabled = true in
# config.toml — see docker/entrypoint.sh). "swu" is the original SWu-IKEv2
# Python dialer (default during the strongSwan proving period,
# specs/012-strongswan-epdg); "strongswan" is the new engine with proper
# IKE rekeying/re-auth/DPD/MOBIKE and a network namespace that survives
# reconnects. Switching engines is a restart, no rebuild.
# TUNNEL_ENGINE=swu
